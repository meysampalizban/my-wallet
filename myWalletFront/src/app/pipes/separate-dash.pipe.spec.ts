import { SeparateDashPipe } from './separate-dash.pipe';

describe('SeparateDashPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateDashPipe();
    expect(pipe).toBeTruthy();
  });
});
